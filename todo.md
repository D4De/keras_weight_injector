# TODO list
### general
- [ ] properly handle and document the two `main`s

### injector
- [ ] compute metrics for each batch rather than on the entire dataset

### metrics
- [ ] reimplement the image classification metrics using tf.Tensor
- [ ] reorganize the metrics in general

### reports
- [ ] properly handle different metrics
- [ ] avoid writing "nan" for each NaN (using pandas instead of csv or None instead of NaNs)

### Integrate the segmentation model
- [ ] merge the two main files as soon as you have restructured the entire project

### tags and releases
- [ ] create a release before starting the new project
- [ ] create a relese after the end of the new project

