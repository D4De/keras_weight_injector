# Dummy Example

This example generates fake data trying to simulate the same shape the data would have if it was an image segmentation task. 
It is useful because it can be executed on he cpu without the real dataset. Also readable, so that to understand the codebase.
